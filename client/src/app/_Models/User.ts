export interface User {
    id: string;
    pen_name: string;
    avatar: string;
}

export interface UserSettings {
    email: string;
    username: string;
    first_name: string;
    last_name: string;
}
